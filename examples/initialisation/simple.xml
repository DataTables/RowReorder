<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="sequence" table-class="display" order="1">

<css lib="datatables rowreorder" />
<js lib="jquery datatables rowreorder">
<![CDATA[

$(document).ready(function() {
	var table = $('#example').DataTable( {
		rowReorder: true
	} );
} );

]]>
</js>

<title lib="RowReorder">Basic initialisation</title>

<info><![CDATA[

This trivial example shows the use of the `rr-init rowReorder` property being used to enable RowReorder on a DataTable. The first column in the table is a sequence number that provides the basis for the ordering. To change a row's order, simply click and drag the row.

Note that sorting is enabled on all columns in this example - you may wish to [restrict sorting to just the sequence column](restrictedOrdering.html) to help prevent potential user confusion if they reorder when the table ordering is performed on a different column.

]]></info>

</dt-example>

